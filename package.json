{
  "name": "work-search-reporter",
  "version": "1.0.0",
  "private": true,
  "description": "Fetch Gmail application confirmation emails and missing company data for an Idaho work-search CSV.",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "collect": "ts-node --project tsconfig.json src/cli.ts",
    "count": "ts-node --project tsconfig.json src/countCli.ts",
    "enrich": "ts-node --project tsconfig.json src/enrichCli.ts",
    "start": "ts-node --project tsconfig.json src/menuCli.ts",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "dotenv": "^17.4.2",
    "googleapis": "^173.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
